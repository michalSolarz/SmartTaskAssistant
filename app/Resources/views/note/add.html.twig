{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(form) }}
    {{ form_errors(form) }}

    {{ form_row(form.content | escape) }}

    <input type="submit"/>
    {{ form_end(form) }}
{% endblock %}